get:
  summary: Get entity vitals
  tags: [Entities]
  operationId: getVitals
  parameters:
    - name: id
      in: query
      required: true
      schema:
        type: string
    - name: ryear
      in: query
      required: true
      schema:
        type: string
    - name: option
      in: query
      schema:
        type: boolean
    - name: poverty
      in: query
      schema:
        type: boolean
  responses:
    '200':
      description: Entity vitals
      content:
        application/json:
          schema:
            $ref: '../schemas/ArenewEntity.yaml'
